General Responsibilities:
- All members must be able to operate Marvin v1 independently and competently.

# TODO
### Akshart
- Validate the new `/goal_point` override and `/goal_point/add` queue flow on hardware
- Decide whether startup file loading should default to `queue` or `override` for field runs
- Tune `global_throttle_scale` and sample `goal_inputs.defaults.yaml` values against real rover response
- Confirm `camera_pose_topic` matches the deployed ZED topic name on the active rover
- Add a small operator test checklist for queue clearing, file-goal loading, and per-goal throttle

### Ben
- Finish mapping software
- Data throughput monitoring

### Pat
- Sort out teensy code for science and arm

### Pratham
- Integrate arm UI into GUI
- Buttons to open different payloads

### Jackson
- Learn teensy code
